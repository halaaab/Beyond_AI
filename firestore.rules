rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /libraries/{document=**} {
      allow read: if true;
      allow write: if true; // Temporarily enable for seeding - disable after!
    }
  }
}
